import EnterPensionClient from "./components/enter-pension-client";

export const metadata = {
  title: "Enter Pension",
};

const page = () => {
  return <EnterPensionClient />;
};

export default page;
