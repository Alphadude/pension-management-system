export const metadata = {
  title: "Reports",
};
const ReportsPage = async () => {
  // const session = await auth();
  return <>{/* <GenerateReportsModal {...{ session }} /> */}</>;
};

export default ReportsPage;
